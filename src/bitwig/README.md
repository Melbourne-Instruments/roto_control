# Melbourne Instruments Bitwig Extensions

The extension here are:

- ROTO Control

To build you need Java 21 sdk and maven. Go to `/src/Bitwig`

```
mvn install
```

You will then find `Roto-Control_v2.bwextension` in the target folder. Copy that to or user Bitwig Folder `/Documents/Bitwig Studio/Extensions/`

